<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimpleMoney8">
        <activity
            android:name=".View.SelectPayment"
            android:exported="false"
            android:label="@string/title_activity_select_payment"
            android:theme="@style/Theme.SimpleMoney8" />
        <activity
            android:name=".View.LoginCompose"
            android:exported="false"
            android:label="@string/title_activity_login_compose"
            android:theme="@style/Theme.SimpleMoney8" />
        <activity
            android:name=".View.RegisterEmailAndPassword"
            android:exported="false"
            android:label="@string/title_activity_register_email_and_password"
            android:theme="@style/Theme.SimpleMoney8" />
        <activity
            android:name=".View.RegisterCompose"
            android:exported="false"
            android:label="@string/title_activity_register_compose"
            android:theme="@style/Theme.SimpleMoney8" />
        <activity
            android:name=".NotifyActivity"
            android:exported="false" />
        <activity
            android:name=".View.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.SimpleMoney8">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>